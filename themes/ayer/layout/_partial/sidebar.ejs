<button class="navbar-toggle"></button>
<nav class="navbar">
  <% if (theme.logo){ %>
  <div class="logo">
    <a href="<%- url_for() %>"><img src="<%- theme.logo %>" alt="<%= config.title %>"></a>
  </div>
  <% } %>
  <!-- 上组：技术文章与自我介绍（主页 → 关于） -->
  <ul class="nav nav-main">
    <% for (var i in theme.menu){ if (i === '成果展示') break; %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } %>
  </ul>
  <!-- 搜索：位于上组下方 -->
  <% if (theme.search){ %>
  <a class="nav-item-link nav-item-search nav-search-top" title="<%= __('search') %>">
    <i class="ri-search-line"></i>
  </a>
  <% } %>
  <!-- 中组：成果展示 / 自创游戏 / 音乐盒 -->
  <ul class="nav nav-mid">
    <% var inMid = false; %>
    <% for (var i in theme.menu){ if (i === '成果展示') inMid = true; if (i === '私人空间') break; if (!inMid) continue; %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } %>
  </ul>
</nav>
<!-- 底部：私人空间 -->
<nav class="navbar navbar-bottom">
  <ul class="nav nav-life">
    <% for (var i in theme.menu){ if (i === '私人空间'){ %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } } %>
  </ul>
</nav>
<div class="search-form-wrap">
  <%- partial('post/search') %>
</div>