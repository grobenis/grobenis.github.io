<button class="navbar-toggle"></button>
<nav class="navbar">
  <% if (theme.logo){ %>
  <div class="logo">
    <a href="<%- url_for() %>"><img src="<%- theme.logo %>" alt="<%= config.title %>"></a>
  </div>
  <% } %>
  <!-- 上组：技术文章与自我介绍（主页 → 关于） -->
  <!-- 注：分类/标签入口已收进归档页顶部筛选条；此处显式过滤，
       避免其他机器未跟踪的 themes/ayer/_config.yml 默认值经深合并把菜单补回来 -->
  <ul class="nav nav-main">
    <% for (var i in theme.menu){ if (i === '旅程') break; if (i === '分类' || i === '标签') continue; %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } %>
  </ul>
  <!-- 搜索：位于上组下方 -->
  <% if (theme.search){ %>
  <a class="nav-item-link nav-item-search nav-search-top" title="<%= __('search') %>">
    <i class="ri-search-line"></i>
  </a>
  <% } %>
  <!-- 中组：旅程 / 成果展示 / 自创游戏 / 音乐盒 -->
  <ul class="nav nav-mid">
    <% var inMid = false; %>
    <% for (var i in theme.menu){ if (i === '旅程') inMid = true; if (i === '私人空间') break; if (!inMid) continue; if (i === '分类' || i === '标签') continue; %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } %>
  </ul>
</nav>
<!-- 底部：私人空间（隐藏入口） -->
<nav class="navbar navbar-bottom">
  <ul class="nav nav-life">
    <% for (var i in theme.menu){ if (i === '私人空间'){ %>
    <li class="nav-item">
      <a class="nav-item-link" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
    </li>
    <% } } %>
  </ul>
</nav>
<script>
/* 侧边栏当前页高亮：路径匹配加 is-active；文章页归入「主页」 */
(function () {
  function norm(p) {
    return (p || '/').replace(/\/+$/, '') || '/';
  }
  var here = norm(location.pathname);
  var isPost = /^\/\d{4}\//.test(location.pathname);
  var links = document.querySelectorAll('.nav-item-link[href]');
  Array.prototype.forEach.call(links, function (a) {
    var href = norm(a.getAttribute('href'));
    if (href === here || (isPost && href === '/')) {
      a.classList.add('is-active');
    }
  });
})();
</script>